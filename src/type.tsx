export interface DataType {
  id: number;
  medium_cover_image?: string;
  bg: string;
  isLiked?: boolean;
}

export interface suggType {
  id: number;
  title: string;
  medium_cover_image: string;
}
